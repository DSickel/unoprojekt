# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

#Login
GET		/login						controllers.Application.login()

#Authentifictaion
POST	/login						controllers.Application.startseite()
GET 	/startseite					controllers.Application.start()

#Spiel
GET		/spiel_erstellen			controllers.UnoGame.spiel_erstellen()
GET		/spiel_beitreten			controllers.UnoGame.spiel_beitreten()
GET		/regelwerk					controllers.UnoGame.regelwerk()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
